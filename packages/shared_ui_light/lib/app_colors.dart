import 'package:flutter/material.dart';

class AppColors {
  // Light theme — beyaz üzerine koyu lacivert-gri
  static const Color primary = Color(0xFF1E3A5F);       // Derin lacivert — Ana renk
  static const Color secondary = Color(0xFF2C5282);     // Orta lacivert
  static const Color accent = Color(0xFF3182CE);        // Parlak mavi (buton, vurgu)
  static const Color background = Color(0xFFF7F9FC);    // Çok açık mavi-beyaz
  static const Color cardBackground = Color(0xFFFFFFFF); // Saf beyaz kart
  static const Color surface = Color(0xFFEDF2F7);       // Açık gri yüzey
  static const Color textPrimary = Color(0xFF1A202C);   // Koyu lacivert-gri metin
  static const Color textSecondary = Color(0xFF4A5568); // Orta gri metin
  static const Color textHint = Color(0xFFA0AEC0);      // Açık gri ipucu metni
  static const Color error = Color(0xFFE53E3E);         // Kırmızı
  static const Color success = Color(0xFF38A169);       // Yeşil
  static const Color warning = Color(0xFFDD6B20);       // Turuncu
  static const Color border = Color(0xFFE2E8F0);        // Açık kenarlık
  static const Color divider = Color(0xFFEDF2F7);       // Bölücü çizgi
  static const Color shadow = Color(0x1A000000);        // Hafif gölge
}
