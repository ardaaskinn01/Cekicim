import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF10B981);       // Enerjik Nane Yeşili (Mint Green)
  static const Color secondary = Color(0xFF059669);     // Zümrüt Yeşili (Emerald)
  static const Color accent = Color(0xFF34D399);        // Açık Nane Yeşili
  static const Color background = Color(0xFF0F172A);    // Slate 900 (Koyu lacivert)
  static const Color cardBackground = Color(0xFF1E293B); // Slate 800 (Kart arka planı)
  static const Color surface = Color(0xFF334155);       // Slate 700
  static const Color textPrimary = Color(0xFFF8FAFC);   // Slate 50 (Temiz Parlak Beyaz)
  static const Color textSecondary = Color(0xFF94A3B8); // Slate 400 (Yumuşak açık gri)
  static const Color textHint = Color(0xFF64748B);      // Slate 500 (İpucu metni)
  static const Color error = Color(0xFFF43F5E);         // Gül Kırmızısı
  static const Color success = Color(0xFF10B981);       // Başarılı Yeşili
  static const Color warning = Color(0xFFF59E0B);       // Amber Sarısı
  static const Color border = Color(0xFF334155);        // Slate 700 (Kenarlık)
  static const Color divider = Color(0xFF1E293B);       // Bölücü çizgi
  static const Color shadow = Colors.black38;
}
