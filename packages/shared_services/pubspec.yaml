name: shared_services
description: "Shared Supabase, location and notification services for Çekici app"
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  geolocator: ^13.0.1
  flutter_local_notifications: ^17.2.2
  flutter_dotenv: ^5.1.0

  shared_models:
    path: ../shared_models
